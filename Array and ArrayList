
#Array & ArrayList Program 

import java.util.ArrayList;

public class Array {

	public static void main(String args[])
	{
		String[] fruits = new String[3];
		fruits[0] = "Mango";
		fruits[1] = "Apple";
		fruits[2] = "Strawberry";
		
		System.out.println(fruits[1]);
		
		ArrayList fruitlist = new ArrayList();
		fruitlist.add("Pineapple");
		fruitlist.add("coconut");
		System.out.println(fruitlist);
		fruitlist.remove("coconut");
		System.out.println(fruitlist);
		System.out.println(fruitlist.contains("Pineapple"));
		fruitlist.clear();
		System.out.println(fruitlist);
		System.out.println(fruitlist.contains("Pineapple"));
		
	}
}


Note:

Methods: 
add(value): appends value at the end of the list
add(index,value)
clear(): Removes all the element from the list
indexof(value): -1 if not found
get(index): Returns the value present at the given index
remove(index): Shifts to left and removes the value
set(index,value): Replaces value at the given index with given value.
toString(): returns a string representation of the list such as "[1,2]"


